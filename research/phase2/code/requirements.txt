# Phase 2 — Neural Style Transfer (Gatys + AdaIN)
# Tested with Python 3.11 on Apple Silicon (MPS). CUDA/CPU also work.
torch>=2.2
torchvision>=0.17
pillow>=9.0
numpy>=1.24
matplotlib>=3.7
reportlab>=4.0
